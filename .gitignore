# Environment variables (contain API keys and passwords)
docker/.env

# Synapse homeserver data (signing keys, database, media)
docker/synapse/data/

# Agent runtime state (sessions, Matrix credentials, cron state, workspace)
docker/configs/*/agents/
docker/configs/*/canvas/
docker/configs/*/credentials/
docker/configs/*/cron/
docker/configs/*/matrix/
docker/configs/*/workspace/
docker/configs/*/update-check.json

# ngircd/ergo configs (replaced by Matrix)
docker/ngircd/

# CLI build artifacts and secrets
cli/node_modules/
cli/openhive/node_modules/
cli/dist/
cli/.env

# Shared Matrix homeserver data (signing keys, database, media) and secrets
cli/matrix/data/
cli/matrix/observer.env
cli/matrix/credentials.md

# Experiment run artifacts (generated per-run, not source)
experiments/*/runs/

# All experiment directories (except template and base skills)
experiments/*
!experiments/_template
!experiments/_base-skills
