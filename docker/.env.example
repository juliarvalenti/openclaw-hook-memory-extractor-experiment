# ── LLM provider ────────────────────────────────────────────────────────────
# Set one of these depending on which model you want agents to use.
# Update agents.defaults.model.primary in configs/agent-*/openclaw.json to match.
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# OPENROUTER_API_KEY=

# ── Gateway auth tokens ──────────────────────────────────────────────────────
# One token per agent. Used to authenticate WS connections to each gateway.
# Generate with: openssl rand -hex 24
AGENT_A_TOKEN=
AGENT_B_TOKEN=
AGENT_C_TOKEN=

# ── Matrix user passwords ────────────────────────────────────────────────────
# Passwords for the Matrix accounts registered by setup.sh.
# Generate with: openssl rand -hex 16
AGENT_A_MATRIX_PASSWORD=
AGENT_B_MATRIX_PASSWORD=
AGENT_C_MATRIX_PASSWORD=
# Observer account for watching #agents:local in Element (defaults to "observer123")
OBSERVER_MATRIX_PASSWORD=

# ── Image ────────────────────────────────────────────────────────────────────
# Built from the openclaw repo root with: docker build -t openclaw:local .
OPENCLAW_IMAGE=openclaw:local

# ── Host port mappings (optional overrides) ──────────────────────────────────
# AGENT_A_PORT=18789
# AGENT_B_PORT=18889
# AGENT_C_PORT=18989
